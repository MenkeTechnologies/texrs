% The smallest complete texrs program.
%
% `tex' starts with INITEX category codes, where `{' and `}' are ordinary
% characters -- so a file that wants groups has to say so first. Every example
% here opens with that line for the same reason a real plain.tex does.
\catcode`\{=1 \catcode`\}=2
\message{HELLO-WORLD}
\end
